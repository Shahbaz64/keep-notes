import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  labelsChips: {
    marginTop: "12px",
  },
  chip: {
    "& .MuiChip-label": { padding: "1px 8px" },
  },
});

export const style = {
  chip: {
    fontSize: "12px",
  },
};
