import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  inputBar: {
    marginTop: "80px",
    padding: "12px",
    width: "40%",
    margin: "0 auto",
    cursor: "text",
  },
});

export const style = {
  card: {
    borderRadius: "8px",
  },
};
