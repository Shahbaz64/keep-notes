import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  labels: {
    marginTop: "10px",
  },
});

export const style = {
  input: {
    marginLeft: "10px",
    marginRight: "10px",
  },
};
