import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  labels: {
    marginTop: "10px",
  },
});

export const style = {
  input: {
    marginLeft: "12px",
    marginRight: "12px",
  },
};
