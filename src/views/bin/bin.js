import { Typography } from "@mui/material";
import React from "react";

const Bin = () => {
  return (
    <div>
      <Typography variant="h3">its bin</Typography>
    </div>
  );
};

export default Bin;
